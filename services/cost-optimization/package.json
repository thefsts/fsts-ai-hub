{
  "name": "@fsts/cost-optimization",
  "version": "0.1.0",
  "private": true,
  "description": "Cost Optimization Engine — measures, governs, forecasts, and actively reduces AI costs across FSTS-owned systems and authorized client environments.",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint: cost-optimization (no-op)\"",
    "test": "vitest run",
    "test:security": "vitest run",
    "test:isolation": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@fsts/contracts": "workspace:*",
    "@fsts/security": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
