{
  "name": "@fsts/security",
  "version": "0.1.0",
  "private": true,
  "description": "Deny-by-default authorization, tenant isolation, and allowlist enforcement for FSTS AI Hub.",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint: security (no-op)\"",
    "test": "vitest run",
    "test:security": "vitest run",
    "test:isolation": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@fsts/contracts": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
